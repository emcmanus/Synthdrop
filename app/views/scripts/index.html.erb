<% content_for :title, "Jams" %>
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <h3>
        Your Tracks <small>(<%= number_with_delimiter @script_count %>)</small>
        <div class="pull-right">
          <%= link_to "New", new_script_path, class: "btn btn-primary btn-sm" %>
        </div>
      </h3>
      <hr/>
      <%= render current_user.scripts %>
    </div>
  </div>
</div>
