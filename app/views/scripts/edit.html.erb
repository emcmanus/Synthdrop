<% content_for :title, "Edit #{@script.title}" %>

<div class="row">
  <div class="col-md-6 col-md-push-3" style="padding-top: 30px;">
    <h3> Edit <%= @script.title %> </h3>
    <%= render 'form' %>
    <br />
    <%= link_to "Delete", script_path(@script), method: :delete, data: {confirm: "Are you sure? This can't be undone."}, class: 'text-warning' %>
  </div>
</div>
